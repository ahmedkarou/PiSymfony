<form method="post" action="{{ path('app_participatient_delete', {'id': participatient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participatient.id) }}">
    <button class="btn">Delete</button>
</form>
